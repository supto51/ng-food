import { LottiePlayerDirective } from './lottie-player.directive';

describe('LottiePlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new LottiePlayerDirective();
    expect(directive).toBeTruthy();
  });
});
